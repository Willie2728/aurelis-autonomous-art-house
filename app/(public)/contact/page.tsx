import { Mail, MapPin, MessageCircle, Phone } from "lucide-react";
import { PageHero, DemoNotice } from "@/components/site/page-elements";
import { ContactForm } from "@/components/site/contact-form";
export const metadata = { title: "Contact" };
export default async function ContactPage({ searchParams }: { searchParams: Promise<{ subject?: string }> }) { const query = await searchParams; const initial = query.subject === "design-consultation" ? "Design consultation" : "Private consultation"; return <><PageHero compact eyebrow="Collector care" title="Let’s make room for the conversation." description="Speak with a human advisor about a work, placement, a private commission, an institutional collection, or anything Seraphina should not decide alone."/><DemoNotice>Contact delivery is simulated. No message leaves this browser.</DemoNotice><section className="px-5 py-20 lg:px-10"><div className="mx-auto grid max-w-6xl gap-10 lg:grid-cols-[.65fr_1.35fr]"><div><p className="eyebrow text-[#c8a968]">Private appointments</p><h2 className="mt-4 font-display text-4xl">Collector care, by design.</h2><p className="mt-5 text-sm leading-7 text-white/45">Use the form for a tracked demo handoff. In production, urgent and unacknowledged requests can trigger repeated human notifications until confirmed.</p><div className="mt-9 space-y-5 text-xs text-white/50"><p className="flex gap-3"><MessageCircle size={16} className="text-[#c8a968]"/>Private viewing & advisory</p><p className="flex gap-3"><Mail size={16} className="text-[#c8a968]"/>concierge@aurelis.example</p><p className="flex gap-3"><Phone size={16} className="text-[#c8a968]"/>Voice channel awaiting provider</p><p className="flex gap-3"><MapPin size={16} className="text-[#c8a968]"/>Virtual estate · worldwide</p></div></div><ContactForm initialSubject={initial}/></div></section></>; }
