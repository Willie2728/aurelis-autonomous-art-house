import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
import { PageHero } from "@/components/site/page-elements";
import { collections, artworks } from "@/components/site/catalog";
export const metadata = { title: "Collections" };
export default function CollectionsPage() { return <><PageHero compact eyebrow="Curatorial constellations" title="Collections for a way of seeing." description="Each collection draws a line through medium, geography, atmosphere, and idea—never reducing the work to a single trend."/><section className="px-5 py-20 lg:px-10"><div className="mx-auto grid max-w-[1450px] gap-5 md:grid-cols-2">{collections.map((collection,index) => { const work = artworks.find((artwork) => collection.artworkIds.includes(artwork.id)) || artworks[index % artworks.length]; return <Link key={collection.slug} href={`/collections/${collection.slug}`} className={`group relative isolate flex min-h-[470px] flex-col justify-end overflow-hidden p-8 ${index % 5 === 0 ? "md:col-span-2 md:min-h-[570px]" : ""}`}><div className="absolute inset-0 -z-10 bg-cover bg-center transition duration-1000 group-hover:scale-[1.03]" style={{ backgroundImage: `linear-gradient(180deg,rgba(0,0,0,.05),rgba(4,3,2,.88)),url(${work.image})` }}/><div className="flex items-end justify-between gap-6"><div><p className="eyebrow text-[#d9bd7a]">{collection.subtitle}</p><h2 className="mt-4 font-display text-5xl md:text-6xl">{collection.title}</h2><p className="mt-4 max-w-xl text-sm leading-7 text-white/58">{collection.description}</p></div><ArrowUpRight className="shrink-0 text-white/55"/></div></Link>})}</div></section></>; }
