import Link from "next/link";
import { Check } from "lucide-react";
import { PageHero, DemoNotice } from "@/components/site/page-elements";
const plans = [{ name: "Visitor", price: "Complimentary", copy: "For thoughtful wandering.", features: ["Full public gallery access","Save works and rooms","Seraphina guidance"] }, { name: "Circle", price: "$180 / year", copy: "For a closer cultural rhythm.", features: ["Everything in Visitor","Early exhibition access","Members’ digital salons","Preview new living editions"] }, { name: "Collector", price: "$680 / year", copy: "For active collection building.", features: ["Everything in Circle","Annual art advisory session","Private digital viewing room","Preferred edition access"] }, { name: "Patron", price: "$2,400 / year", copy: "For sustained participation.", features: ["Everything in Collector","Quarterly advisory sessions","First access to commissions","Curator-led private programs"] }, { name: "Institutional", price: "$4,800 / year", copy: "For hospitality and enterprise.", features: ["Commercial display planning","Multi-site collection tools","Licensing consultation","Dedicated human relationship lead"] }];
export const metadata = { title: "Membership" };
export default function MembershipPage() { return <><PageHero eyebrow="Belong to the art house" title="Membership as a deeper way of looking." description="Move from discovery to dialogue with private previews, advisory access, digital salons, and collection-building support."/><DemoNotice>Membership enrollment is simulated until authentication, billing, and member-service providers are connected.</DemoNotice><section className="px-5 py-20 lg:px-10"><div className="mx-auto grid max-w-[1450px] gap-4 md:grid-cols-2 xl:grid-cols-5">{plans.map((plan,index) => <article key={plan.name} className={`flex flex-col border p-6 ${index === 2 ? "border-[#c8a968] bg-[#c8a968]/8" : "border-white/12"}`}><div>{index === 2 && <p className="mb-4 text-[9px] uppercase tracking-widest text-[#d9bd7a]">Curator’s choice</p>}<h2 className="font-display text-3xl">{plan.name}</h2><p className="mt-3 text-sm text-[#c8a968]">{plan.price}</p><p className="mt-5 text-xs leading-6 text-white/45">{plan.copy}</p><ul className="mt-7 space-y-4">{plan.features.map((feature) => <li key={feature} className="flex gap-2 text-xs leading-5 text-white/60"><Check size={13} className="mt-0.5 shrink-0 text-[#c8a968]"/>{feature}</li>)}</ul></div>{plan.name === "Visitor" ? <Link href="/account" className="mt-9 border border-white/20 p-3 text-center text-[10px] uppercase tracking-widest">Create demo account</Link> : <Link href={`/checkout?plan=${plan.name}`} className="mt-auto pt-9"><span className={`block p-3 text-center text-[10px] font-bold uppercase tracking-widest ${index === 2 ? "bg-[#c8a968] text-black" : "border border-white/20"}`}>Choose {plan.name}</span></Link>}</article>)}</div></section></>; }
