import Link from "next/link";
import { ArrowRight } from "lucide-react";
import { PageHero } from "@/components/site/page-elements";
import { exhibitions, artworks } from "@/components/site/catalog";
export const metadata = { title: "Exhibitions" };
export default function ExhibitionsPage() { return <><PageHero compact eyebrow="Program" title="Now, next, and still resonating." description="A continuously evolving exhibition program shaped by curatorial intelligence, cultural review, and human oversight."/><section className="px-5 py-20 lg:px-10"><div className="mx-auto max-w-[1350px] space-y-8">{exhibitions.map((exhibition,index) => { const hero = artworks.find((artwork) => exhibition.artworkIds.includes(artwork.id)) || artworks[index]; return <article key={exhibition.slug} className="group grid overflow-hidden border border-white/10 md:grid-cols-[1.1fr_.9fr]"><div className="min-h-[400px] bg-cover bg-center transition duration-1000 group-hover:brightness-110" style={{ backgroundImage: `url(${hero.image})` }}/><div className="flex flex-col justify-between bg-[#13110e] p-8 md:p-12"><div><div className="flex items-center justify-between"><span className="eyebrow text-[#c8a968]">{exhibition.status}</span><span className="text-[10px] text-white/35">{exhibition.dates}</span></div><h2 className="mt-8 font-display text-5xl leading-none">{exhibition.title}</h2><p className="mt-6 text-sm leading-7 text-white/52">{exhibition.statement}</p></div><Link href={`/exhibitions/${exhibition.slug}`} className="mt-10 flex items-center gap-3 text-[10px] uppercase tracking-widest text-[#d9bd7a]">Enter exhibition<ArrowRight size={15}/></Link></div></article>})}</div></section></>; }
